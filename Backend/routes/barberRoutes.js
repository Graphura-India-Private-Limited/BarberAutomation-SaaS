const express=require("express");const router=express.Router();const bcrypt=require("bcryptjs");const Barber=require("../models/Barber");const Queue=require("../models/Queue");const Booking=require("../models/Booking");const BreakRequest=require("../models/BreakRequest");const{protect}=require("../middleware/authMiddleware");
router.get("/salon/:salon_id",async(req,res)=>{try{const barbers=await Barber.find({salon_id:req.params.salon_id,is_active:true});res.json({success:true,barbers});}catch(err){res.status(500).json({success:false,message:err.message});}});
router.post("/",protect,async(req,res)=>{try{const{salon_id,name,mobile,password,specialization,experience}=req.body;const exists=await Barber.findOne({mobile});if(exists)return res.status(400).json({success:false,message:"Mobile exists"});const password_hash=await bcrypt.hash(password,10);const barber=await Barber.create({salon_id,name,mobile,password_hash,specialization,experience});res.status(201).json({success:true,barber});}catch(err){res.status(500).json({success:false,message:err.message});}});
router.put("/:id/status",protect,async(req,res)=>{try{const{status}=req.body;await Barber.findByIdAndUpdate(req.params.id,{status});if(status==="break"||status==="offline"){await Queue.updateMany({barber_id:req.params.id,status:"waiting"},{status:"paused"});}if(status==="available"){await Queue.updateMany({barber_id:req.params.id,status:"paused"},{status:"waiting"});}res.json({success:true,message:`Status: ${status}`});}catch(err){res.status(500).json({success:false,message:err.message});}});
router.get("/:id/dashboard",protect,async(req,res)=>{try{const barber=await Barber.findById(req.params.id).populate("salon_id","salon_name address");if(!barber)return res.status(404).json({success:false,message:"Not found"});const today=new Date();today.setHours(0,0,0,0);const todayQueue=await Queue.find({barber_id:req.params.id,status:{$in:["waiting","in-progress","paused","delayed"]},joined_at:{$gte:today}}).populate("customer_id","name mobile").populate("booking_id","booking_type total_amount services").sort({position:1});const completedToday=await Queue.countDocuments({barber_id:req.params.id,status:"completed",joined_at:{$gte:today}});const totalServed=await Queue.countDocuments({barber_id:req.params.id,status:"completed"});res.json({success:true,barber,todayQueue,stats:{completedToday,totalServed,rating:barber.rating}});}catch(err){res.status(500).json({success:false,message:err.message});}});
router.put("/:barber_id/queue/:queue_id/start",protect,async(req,res)=>{try{await Queue.findByIdAndUpdate(req.params.queue_id,{status:"in-progress"});await Barber.findByIdAndUpdate(req.params.barber_id,{status:"busy"});const q=await Queue.findById(req.params.queue_id);await Booking.findByIdAndUpdate(q.booking_id,{status:"in-progress"});res.json({success:true,message:"Service started!"});}catch(err){res.status(500).json({success:false,message:err.message});}});
router.put("/:barber_id/queue/:queue_id/complete",protect,async(req,res)=>{try{const q=await Queue.findByIdAndUpdate(req.params.queue_id,{status:"completed",served_at:new Date()},{new:true});await Barber.findByIdAndUpdate(req.params.barber_id,{status:"available"});await Booking.findByIdAndUpdate(q.booking_id,{status:"completed"});await Queue.updateMany({barber_id:req.params.barber_id,status:"waiting",position:{$gt:q.position}},{$inc:{position:-1}});res.json({success:true,message:"Service completed!"});}catch(err){res.status(500).json({success:false,message:err.message});}});
router.put("/:barber_id/queue/:queue_id/noshow",protect,async(req,res)=>{try{const q=await Queue.findByIdAndUpdate(req.params.queue_id,{status:"noshow"},{new:true});await Booking.findByIdAndUpdate(q.booking_id,{status:"noshow"});await Barber.findByIdAndUpdate(req.params.barber_id,{status:"available"});await Queue.updateMany({barber_id:req.params.barber_id,status:"waiting",position:{$gt:q.position}},{$inc:{position:-1}});res.json({success:true,message:"No-show marked"});}catch(err){res.status(500).json({success:false,message:err.message});}});
router.post("/:id/break-request",protect,async(req,res)=>{try{const{break_type,start_time,duration_mins,reason}=req.body;const autoApprove=break_type==="short"||Number(duration_mins)<30;const breakReq=await BreakRequest.create({barber_id:req.params.id,break_type:break_type||"short",start_time:start_time||new Date(),duration_mins:duration_mins||15,reason:reason||"",status:autoApprove?"approved":"pending"});if(autoApprove){await Barber.findByIdAndUpdate(req.params.id,{status:"break"});}res.json({success:true,break:breakReq,autoApproved:autoApprove});}catch(err){res.status(500).json({success:false,message:err.message});}});
router.put("/:id/break-end",protect,async(req,res)=>{try{await Barber.findByIdAndUpdate(req.params.id,{status:"available"});await BreakRequest.findOneAndUpdate({barber_id:req.params.id,status:"approved",end_time:null},{status:"ended",end_time:new Date()});await Queue.updateMany({barber_id:req.params.id,status:"paused"},{status:"waiting"});res.json({success:true,message:"Break ended!"});}catch(err){res.status(500).json({success:false,message:err.message});}});
router.put("/:id",protect,async(req,res)=>{try{const barber=await Barber.findByIdAndUpdate(req.params.id,req.body,{new:true});res.json({success:true,barber});}catch(err){res.status(500).json({success:false,message:err.message});}});
router.delete("/:id",protect,async(req,res)=>{try{await Barber.findByIdAndUpdate(req.params.id,{is_active:false});res.json({success:true,message:"Deactivated"});}catch(err){res.status(500).json({success:false,message:err.message});}});
module.exports=router;
